<resources>
    <!-- Base application theme. -->
    <style name="Theme.SimpleTasks" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="backgroundColor">@color/white</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="hintTextColor">@color/grey</item>
        <!-- button styles -->
        <item name="buttonBarPositiveButtonStyle">@color/green</item>
        <item name="buttonBarNegativeButtonStyle">@color/red</item>
        <item name="buttonBarNeutralButtonStyle">@color/light_red</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/accent_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- error color -->
        <item name="colorError">@color/red</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/bordeaux</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.SimpleTasks.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowActionBarOverlay">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.SimpleTasks.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.SimpleTasks.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>